# React + @sharp9/ofdjs Example

## Setup

```bash
npm install
npm run dev
```

## What it does

- Opens an OFD file via `<input type="file">`
- Parses it with `readOfd`
- Renders the first page to a `<canvas>` using `renderPageToCanvas`
- Supports multi-page navigation (prev/next buttons)
